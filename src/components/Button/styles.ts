import styled from "styled-components";

const StyledButton = styled.button`
  font-size: 16px;
  line-height: 19px;
  color: #fff;

  padding: 15px 147px;

  background: #23c9ff;
  border-radius: 10px;

  cursor: pointer;
`;

export { StyledButton };
